# GitHub Codespaces Configuration

هذا المجلد يحتوي على تكوين GitHub Codespaces لمشروع SA - منصة تحويل النصوص إلى وسائط متعددة.

## 📋 المحتويات

- **`devcontainer.json`**: التكوين الرئيسي لـ Codespaces
- **`Dockerfile`**: صورة Docker المخصصة للبيئة
- **`post-create.sh`**: سكريبت يعمل بعد إنشاء الـ Codespace

## 🚀 كيفية الاستخدام

### 1. فتح في Codespace

من صفحة المستودع على GitHub:

1. اضغط على زر **Code** (أخضر)
2. اختر تبويب **Codespaces**
3. اضغط على **Create codespace on main** (أو الفرع المطلوب)

أو استخدم الرابط المباشر:
```
https://github.com/codespaces/new?hide_repo_select=true&ref=main&repo=YOUR_REPO_ID
```

### 2. الانتظار حتى يتم الإعداد

سيقوم Codespace تلقائياً بـ:
- تثبيت Python 3.11
- تثبيت Poetry وجميع التبعيات
- تكوين امتدادات VS Code
- إنشاء ملف `.env` من القالب
- تثبيت pre-commit hooks

### 3. إضافة مفاتيح API

بعد فتح Codespace، عدّل ملف `.env`:

```bash
nano .env
```

أضف مفاتيحك:
```env
OPENAI_API_KEY=your_key_here
REPLICATE_API_TOKEN=your_token_here
ELEVENLABS_API_KEY=your_key_here
```

**💡 نصيحة:** يمكنك استخدام GitHub Secrets لحفظ المفاتيح بشكل آمن.

### 4. بدء التطوير

```bash
# تشغيل واجهة الويب
make run-ui

# تشغيل API
make run-api

# تشغيل الاختبارات
make test
```

## 🔧 الميزات المضمنة

### امتدادات VS Code المثبتة تلقائياً:
- ✅ Python + Pylance (دعم لغة Python)
- ✅ Black Formatter (تنسيق الكود)
- ✅ Ruff (فحص الجودة)
- ✅ GitLens (أدوات Git متقدمة)
- ✅ GitHub Copilot (المساعد الذكي)
- ✅ Error Lens (عرض الأخطاء مباشرة)
- ✅ TODO Tree (تتبع المهام)

### الإعدادات المطبقة:
- تنسيق تلقائي عند الحفظ
- فحص الكود مع Ruff
- دعم pytest للاختبارات
- حد أقصى لطول السطر: 100 حرف

### المنافذ المفتوحة تلقائياً:
- **8501**: واجهة Streamlit
- **8000**: FastAPI Backend

## 🎯 الأوامر المفيدة

```bash
# عرض جميع الأوامر المتاحة
make help

# تنسيق الكود
make format

# فحص الكود
make lint

# تشغيل الاختبارات مع التغطية
make test-coverage

# تنظيف الملفات المؤقتة
make clean
```

## 🐛 حل المشاكل

### المشكلة: فشل تثبيت التبعيات
```bash
poetry install --no-interaction
```

### المشكلة: الامتدادات لم تُثبّت
1. افتح Command Palette (Ctrl+Shift+P)
2. ابحث عن "Extensions: Show Recommended Extensions"
3. ثبّت الامتدادات المقترحة

### المشكلة: المنافذ غير متاحة
تحقق من تبويب "Ports" في VS Code السفلي وتأكد من:
- Port 8501 للـ Streamlit
- Port 8000 للـ FastAPI

## 📚 مصادر إضافية

- [GitHub Codespaces Docs](https://docs.github.com/en/codespaces)
- [Dev Container Specification](https://containers.dev/)
- [SA Project Documentation](../README.md)

## 💡 نصائح للأداء الأفضل

1. **استخدم machine type مناسب**: اختر 4-core أو أكثر للعمل مع الفيديو
2. **وقف Codespace عند عدم الاستخدام**: لتوفير الموارد
3. **استخدم Secrets لـ API Keys**: أكثر أماناً من `.env`
4. **فعّل pre-commit hooks**: لضمان جودة الكود

---

**صُنع بـ ❤️ لمشروع SA**
